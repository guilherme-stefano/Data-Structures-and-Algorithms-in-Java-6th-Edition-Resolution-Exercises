package R313;

public class R313 {
	//The shallow equality will compare if a[i] == b[i], but if the array is bidimenssional, 
	//this will not work. 
	// To compare two dimensional arrays we need to use the deep equals funcition, that will compare
	// a[i][j] == b[i][j]
}
